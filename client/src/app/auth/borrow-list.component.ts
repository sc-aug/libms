import { Component } from '@angular/core';

import { AuthService } from './auth.service';
@Component({
  selector: 'app-borrow-list',
  template: 'borrow-list.component.html'
})
export class BorrowListComponent {
}
